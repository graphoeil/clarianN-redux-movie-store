/** @type {import('next').NextConfig} */
const nextConfig = {
	// Set to false (it's like <React.StrictMode>)
	// Will call methods (like console) twice in dev mode ;-)
	reactStrictMode:false
};

// Exports
module.exports = nextConfig;